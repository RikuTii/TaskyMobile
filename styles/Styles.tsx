import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
   flexRow: {
    flexDirection: 'row',
   },
   center: {
    alignItems: 'center',
    justifyContent: 'center'
   }
  });